// Sort the Stack

// Given a stack, sort the elements in the stack using one additional stack.

// Example:

// Input: [3, 1, 2, 4]

// Output: [4, 3, 2, 1]

function sortStack(stack: number[]): number[] {
  const sortedStack: number[] = [];
  return sortedStack;
}

console.log(sortStack([3, 1, 2, 4]));
